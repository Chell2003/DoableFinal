@model IEnumerable<DoableFinal.Models.HomePageSection>
@{
    ViewData["Title"] = "Manage Homepage Content";
}

<div class="container-fluid px-4">
    <h1 class="mt-4">Manage Homepage Content</h1>
    <div class="card mb-4">
        <div class="card-header">
            <i class="bi bi-pencil me-1"></i>
            Edit Homepage Sections
        </div>
        <div class="card-body">
            @if (Model == null || !Model.Any())
            {
                <div class="alert alert-info">
                    No sections found. Homepage sections will be created when first accessed.
                </div>
            }
            else
            {
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead class="table-light">
                            <tr>
                                <th>Display Name</th>
                                <th>Key</th>
                                <th>Last Updated</th>
                                <th>Actions</th>
                            </tr>
                                        
                        <tbody>
                            @foreach (var item in Model.OrderBy(s => s.SectionOrder))
                            {
                                <tr>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            @if (!string.IsNullOrEmpty(item.ImagePath))
                                            {
                                                <img src="@item.ImagePath" alt="thumb" class="me-3" style="width:60px; height:60px; object-fit:cover; border-radius:4px;" />
                                            }
                                            <div>
                                                <strong>@item.DisplayName</strong>
                                            </div>
                                        </div>
                                    </td>
                                    <td>
                                        <code>@item.SectionKey</code>
                                    </td>
                                    <td>
                                        @if (item.UpdatedAt.HasValue)
                                        {
                                            <span class="badge bg-info">@item.UpdatedAt.Value.ToString("g")</span>
                                        }
                                        else
                                        {
                                            <span class="text-muted">Never</span>
                                        }
                                    </td>
                                    <td>
                                        <a asp-action="EditHomePageSection" asp-route-id="@item.Id" class="btn btn-sm btn-primary">
                                            <i class="bi bi-pencil"></i> Edit
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
        </div>
    </div>
</div>
